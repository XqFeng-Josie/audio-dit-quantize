# seed_repro — pinned deps (Python 3.13, A100-40GB / sm_80). Reproducible env:
#   python3.13 -m venv .venv && source .venv/bin/activate
#   pip install -r requirements.txt
# NOTE: the cloned model repo (LongCat-AudioDiT) and eval/seed-tts-eval ship their own
# requirements.txt — install those too (see README "Setup"). This file is seed_repro's own stack.

# --- core (also covered by the model-repo requirements; pinned here so this file is self-sufficient) ---
# torch/torchaudio: CUDA build must match the host. If the default PyPI wheel doesn't fit your CUDA,
# install from the matching index, e.g.:  pip install torch==2.12.0 torchaudio==2.11.0 --index-url https://download.pytorch.org/whl/cu124
torch==2.12.0
torchaudio==2.11.0
transformers==5.11.0
safetensors==0.8.0
huggingface-hub==1.19.0
numpy==2.4.6
einops==0.8.2
librosa==0.11.0
soundfile==0.14.0
tqdm==4.67.3

# --- quantization / efficiency ---
torchao==0.17.0          # real INT8 W8A8 (precision.py, profile_efficiency.py)

# --- Seed-tts-eval metrics: CER/WER (zh paraformer + en whisper) and SIM (WavLM via s3prl) ---
jiwer==4.0.0
zhon==2.1.1
zhconv==1.4.3
funasr==1.3.9
modelscope==1.37.1
s3prl==0.4.18
fire==0.7.1

# --- no-reference MOS (evaluate_seedtts_metrics.sh): UTMOS via torch.hub + DNSMOS via speechmos/onnxruntime ---
speechmos==0.0.1.1
onnxruntime==1.27.0

# --- tooling: data download + dashboard xlsx reader ---
gdown==6.1.0
openpyxl==3.1.5          # build_dashboard.py reads docs/*.xlsx
