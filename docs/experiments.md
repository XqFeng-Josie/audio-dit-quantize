# 实验文档：面向任务特征的音频 DiT 量化标定数据选择

> 本文档是本项目的**长期维护实验档案**，结构固定为五部分：**背景 / 研究计划 / 实验计划 / 实验结果与分析 / 其他**。
> 面向读者：不要求语音背景。新增实验：在 §3.4 路线图领 ID → 结果与指标表写进 §4 → 工程/协议变更记入 §5.2。不要删除历史结论（错误结论标注"已推翻"并指向推翻它的实验）。

---

## 1. 背景

我们研究：把一个大型语音生成模型压缩到 4-bit（W4A4 量化）时，**用哪些数据做"标定"效果最好**——并提出从任务特征（文本难度、说话人、prompt/生成区域、扩散时间步）出发**自动选择标定数据**的方法。

### 1.1 任务：零样本声音克隆 TTS

给模型两样东西：一段某说话人的**参考音频**（prompt，约几秒）+ 一句**目标文本**，模型直接合成"这个人说这句话"的音频——不需要针对该说话人训练。衡量好坏主要看两点：**说得对不对**（合成音频转回文字后错字率低）和**像不像这个人**（音色相似度高）。

### 1.2 被量化的模型：LongCat-AudioDiT

美团 LongCat 团队的开源扩散 TTS 模型（[arXiv:2603.29339](https://arxiv.org/abs/2603.29339)，HF: `meituan-longcat/LongCat-AudioDiT-{1B,3.5B}`），在 Seed 基准上是 SOTA 水平。关键构件：

| 构件 | 说明 |
|---|---|
| Wav-VAE | 把 24 kHz 波形压缩成"波形隐空间"序列（不经过 mel 频谱，这是该模型的核心创新） |
| DiT 主干 | Diffusion Transformer，在隐空间做扩散去噪。1B 版：24 层 block，隐维 1536；每 block = self-attn + cross-attn + FFN，AdaLN 调制 |
| 文本编码器 | UMT5-base，把目标文本编成 cross-attn 的条件 |
| 采样 | 流匹配 ODE，**16 步**去噪（nfe=16），APG 引导（强度 4.0） |

推理时序列由两段拼接：**prompt 区**（参考音频的隐向量，负责"像谁"）+ **生成区**（被去噪合成的部分，负责"说什么"）。这个双区结构 + 16 个去噪时间步，是本项目"任务特征"的主要来源。

**量化只针对 DiT 主干的 GEMM 线性层**（self_attn / cross_attn / ffn 的投影矩阵）；VAE、文本编码器、embedding 等保持原精度。

### 1.3 量化与"标定"

- **W4A4**：权重（Weight）和激活（Activation）都用 4-bit 整数表示。数值只剩 16 个档位，误差主要来自少数数值极大的"离群通道"。
- **PTQ（训练后量化）**：不重训模型，只用**少量数据**（标定集，本项目 32 条）跑前向，据此确定量化参数（缩放、裁剪阈值、旋转/变换矩阵等）。
- **标定数据的作用**：量化参数是对着标定数据的激活分布优化出来的。标定集覆盖不了真实使用场景的分布，量化模型就会在没见过的分布上出错。**"选哪 32 条数据"因此是一个自由度很大、但长期被随意处理的设计选择——这正是本项目的研究对象。**

---

## 2. 研究计划

### 2.1 研究问题

> 在音频 DiT 的 PTQ 中，能否**从模型和任务的角度自动选择标定数据**（替代随机抽取/手工启发式），带来可证实的收益——**质量**（超过随机分布均值）、**效率**（更小标定预算达到同等质量）或**鲁棒性**（避开最差随机抽签）三档主张至少其一？

### 2.2 贡献主张

1. **主线：任务感知的标定数据选择**。定义便宜的逐条/逐集合代理分数（模型侧：激活统计、任务代理梯度影响力；任务侧：音素覆盖、文本难度、说话人多样性、时间步覆盖），做集合级（多样性感知）选择，超过随机与手工基线。
2. **副线：任务敏感度加权的标定损失**。用任务代理 loss 的梯度统计给 block 重建损失做 token×通道联合加权（region 加权与通道加权的统一框架）。
3. 两条线共用同一套**敏感度统计基础设施**（Fisher/梯度平方），按通道聚合 → loss 权重，按样本聚合 → 数据影响力分数。

### 2.3 统一叙事主张（2026-07-19 定稿）

两个贡献不是两个孤立技巧，而是同一条原理的两个投影：

> **TTS 的量化退化沿任务轴各向异性**——内容可懂度(CER) vs 音色(SIM)、prompt 区 vs 生成区、早期 vs 晚期扩散步、常规文本 vs 混淆音素。因此标定应当把**数据分布**与**目标函数**同时对齐到任务的敏感度结构（*task-sensitivity-aligned calibration*）：选数据 = 对齐 P(标定数据)，加权 loss = 对齐 L(标定目标)，两者共享同一套敏感度测量基础设施（E2）。

这也是与近邻工作的分界线：LLM/图像扩散的标定研究没有双区、没有说话人空间、没有 SIM/CER 的任务指标分解。首轮 GATE-B 结果（§4.2）已给出各向异性的第一块证据：hard CER 对标定数据高度敏感，SIM 完全不敏感。

### 2.4 相关工作雷达（2026-07-19 调研，投稿前需复查更新）

| 工作 | 内容 | 与我们的差异 |
|---|---|---|
| PTQ for Audio Diffusion Transformers（WASPAA 2025，[arXiv:2510.00313](https://arxiv.org/abs/2510.00313)） | Stable Audio Open 上 W8A8/W4A8 静态 vs 动态基准 + timestep 平滑 + SVD-LoRA 分支 | 最近的音频工作，但是通用音频非零样本 TTS、非 W4A4；**无标定数据选择、无任务感知目标、无说话人/内容维度** |
| Gradient-Aligned Calibration（[arXiv:2602.01289](https://arxiv.org/abs/2602.01289)，2026-02） | 学习标定样本权重以对齐跨 timestep 梯度（图像 UNet 基准） | **思想最近的工作**：纯优化视角（梯度对齐），无任务结构。我们的差异化必须押在语音任务轴上，且节奏要快 |
| SelectQ（[MIR 2024](https://www.mi-research.net/article/doi/10.1007/s11633-024-1518-0)） | 逐层激活统计聚类选标定数据（CNN/ImageNet，4-bit ResNet18 top-1 +15%），动机同为"随机标定的激活分布失配" | **激活统计选标定的直系前辈，必引**。差异化：它匹配训练集整体分布（聚类近心），我们对任务难例探针做覆盖（E2 v2 设计 C）；域不同（CNN 分类 vs 音频 DiT） |
| Quant-dLLM / FAIR-Calib（扩散 LLM，2025–26） | masked calibration simulation / 不稳定性重加权 | 佐证"标定分布要与任务/过程对齐"正在成为共识原理；均非语音 |
| Williams & Aletras 2024；Q-Diffusion / PTQ4DM | LLM 标定数据敏感性；扩散 timestep 采样 | 背景文献：证明了方向、留下了语音空白 |

结论：音频 DiT/TTS 上的任务感知标定（数据 + 目标）仍是空白；"标定加权"大方向在升温，**语音任务结构是护城河**。

### 2.5 想法储备（按任务结构轴；★=已进路线图，☆=候选，○=分析章节储备）

| 任务结构 | 想法 | 状态 |
|---|---|---|
| 双区 prompt/gen | region 加权 loss（已实现）；region-Fisher（GATE-A）；**退化各向异性刻画**（首轮结果已入 §4.2） | ★ |
| 扩散 timestep | 标定单位从"条"升级为 (内容, 说话人, timestep) 三元组的联合选择——GATE-B 失败时的退路并入主线 | ☆（待 GATE-B 终判） |
| 说话人空间 | WavLM 嵌入覆盖作为 P2 选择的多样性项（SIM 基础设施现成）；量化公平性分析（哪类声音掉 SIM 最多，性别/口音元数据现成） | ☆ / ○ |
| 音素/语言学 | 音素覆盖、混淆声母密度特征（拼音元数据已有）；MFA 对齐后逐音素 CER 归因（"量化先弄坏什么"） | ☆ / ○ |
| 文本-音频对齐 | cross-attention 图保持 loss（可懂度失败的机制 = 对齐被量化打坏；hard 集重复串最吃对齐） | ☆（GATE-A 证明 loss 轴有效后再上） |
| 声学属性 | 评测轴加 F0 RMSE / 时长漂移 / 语速（librosa 可算） | ○ |
| 标定预算 | **预算悬崖扫描**：集合缩到 16/8/4，找"随机开始崩、选择还能撑"的区间——小效应世界的效率主张，无空结果风险 | ★（并入 P2） |
| 分布尾部 | **鲁棒性主张**：选择 = 避开最差随机抽签（方差收缩/最差情况兜底）——小效应世界里置信度最高的主张 | ★（GATE-B 分析必含） |

### 2.6 量化方法基线与冻结原则（重要纪律）

改进必须建立在强基线上：FlatQuant 冻结为论文最优配置（详表见 `docs/paper-best-alignment.md`），SVDQuant 为第二基线（验证选择方法与量化方法正交），QuaRot 仅作方法选型表参考。**做数据选择实验时，一切贡献旋钮（loss 加权、region 权重）保持关闭**；增益只作为冻结基线之上的增量报告。

| 方法 | 类型 | 角色 | 入口 |
|---|---|---|---|
| FlatQuant | 梯度训练（逐 block 重建 + 可学习 Kronecker 变换 + LWC/LAC/diag） | **主基线**（唯一有显式 loss、对标定数据敏感的方法） | `flatquant_best.py` / `scripts/benchmark/benchmark_flatquant_best_seedtts.sh` |
| SVDQuant | 闭式（低秩分支 + 平滑网格搜索） | 第二基线（泛化性验证） | `svdquant_pipeline.py` / `scripts/benchmark/benchmark_svdquant_seedtts.sh` |
| QuaRot(-GPTQ) | 免训练（Hadamard 旋转 + GPTQ） | 方法选型表参考 | `quarot_linear.py` / `scripts/benchmark/benchmark_quarot_gptq_seedtts.sh` |

FlatQuant 标定流程：32 条标定数据各跑一次全精度推理（16 步 ODE），钩取 block-0 输入，每条沿轨迹取 2 个时间步快照（共 64 条序列）→ 逐 block 训练量化参数（200 步 × mb4，块输出 MSE）→ 冻结。

---

## 3. 实验计划

### 3.1 评测协议

**测试集**：Seed-TTS 官方测试集，三个子集——zh（中文常规，2020 条）、en（英文常规，1088 条）、hard（中文困难：绕口令/生僻组合，400 条）。

**指标**（都是"量化后 vs 全精度"越接近越好）：

| 指标 | 含义 | 工具 | 方向 |
|---|---|---|---|
| CER / WER | 合成音频经 ASR 转文字后的字/词错误率——**"说得对不对"** | Paraformer(zh) / Whisper(en) | ↓ |
| SIM | 合成音频与参考说话人的音色相似度（说话人向量余弦）——**"像不像这个人"** | WavLM 微调模型 | ↑ |
| MOS | 无参考音质分（已移出默认评测，wav 保留可事后补） | UTMOS + DNSMOS | ↑ |

**配对协议**：逐条固定种子（seed = base + 列表内条目序号），量化前后逐条严格配对；结论必须过 `paired_bootstrap.py` 的配对 bootstrap 置信区间。标定自身用 `--calib_seed` 固定。⚠ dev 列表索引 ≠ 全集索引——dev 运行只与其他 dev 运行（含 `p0_fp32` 参照）逐条配对，绝不与旧全集运行配对。

### 3.2 数据治理（本项目的核心协议）

**三层严格分离**：

| 层 | 用途 | 要求 |
|---|---|---|
| 候选池（candidate pool） | 标定数据的来源，选择算法在此池内挑选 | 与测试集**说话人级不相交**（L2，见下） |
| dev 划分 | 选择信号 / 实验间比较。**已冻结（2026-07-18，seed=20260718）**：zh 300/2020 + hard 100/400 + en 200/1088。uid 清单入库 `data/splits/*_dev_uids.txt`（冻结语义：清单存在时 `dev_split.py` 只物化不重采样）；gen/eval 全链路以 `zh_dev/hard_dev/en_dev` 集名使用 | 测试集的固定子集，仅用于开发期 |
| untouched test | 最终报告（集名 `*_heldtest`：zh 1720 / hard 300 / en 888） | 测试集其余部分，报告前绝不触碰 |

**污染等级定义**（数据选择研究必须 L2；方法对比允许 L1 + 记录 caveat）：

- **L0 配对级**：无标定条目与测试条目 (prompt, 文本) 完全相同；
- **L1 成分级**：prompt 音频、目标文本均不与测试集共享；
- **L2 说话人级**：prompt 说话人来自与测试集不同的语料库。

#### 3.2.1 候选池 v1 规格（2026-07-18，实现：`calib/pool.py` 构建 / `calib/audit.py` 审计）

**测量依据**（决定各筛选窗口）：Seed-TTS 测试 prompt 时长 zh p10/p50/p90 = 4.1/4.5/5.5s（几乎全部 ≥4.0s）、en = 3.4/4.5/6.0s；目标文本长度 zh 常规 18–28 字、hard 中位 51 字长尾至 385、en 41–86 字符。AISHELL-3 单句中位仅 3.2s → zh prompt 只取 ≥4s 的长句（≥4s 供给约 1.3 万条，无需拼接）；模型侧 `load_audio` 自动重采样，但池子仍写出裁剪后 24k 单声道副本（去首尾静音 top_db=35、自包含、路径稳定）。

**构成**（`pool_v1`，seed=20260718，共 376 条 / 158 说话人，旋钮均可调）：

| 子集 | 条数 | prompt 来源 | 目标文本来源 | 文本长度窗 |
|---|---|---|---|---|
| zh_normal | 240（120 说话人 × 2） | AISHELL-3，裁剪后 4.0–9.5s，性别轮转分层，每人取最接近 4.5s 的句子 | AISHELL-3 转写（**非 prompt 说话人**的句子，自带拼音）+“。” | 15–35 字 |
| zh_hardlike | 60 | 复用 zh prompt（打乱） | **按真实 hard 集分类学设计的确定性生成器**（三子风格，素材全部来自 AISHELL-3）：twister 词级混淆绕口令（高频二字词 + 同音/z-zh/n-l/in-ing 等混淆配对 + 句法模板，功能字停用表过滤）/ repeat 句子与片段重复 4–6 次 / concat 多句拼接长句；子风格记入元数据 `hard_substyle` | 25–120 字 |
| en_normal | 76（38 说话人 × 2） | LibriTTS dev-clean，裁剪后 3.0–9.5s | LibriTTS normalized 文本（非 prompt 说话人） | 40–90 字符 |

**交叉配对协议**（镜像测试集结构）：目标文本绝不来自 prompt 自身的句子或说话人；每个 prompt 音频至多用于一个 normal 条目（hardlike 可复用 prompt，审计仅提示不报错）；目标文本池内唯一。

**元数据表**（`pool_v1_meta.csv`，即 P1 任务侧特征矩阵）：说话人/性别/年龄段/口音（AISHELL-3 spk-info 免费提供）、prompt 时长与 RMS 响度、文本来源、字数、音节数、去调唯一音节数、最大同音节重复次数、唯一声母/韵母数、唯一字符比。

**审计政策**（`calib/audit.py`，不通过则池子不可用）：A 音频不得位于 seedtts_testset 内；B prompt 文件名不得与测试 prompt 碰撞；C 归一化文本不得与任何测试 prompt/目标文本相等；D ≥10 字的归一化文本不得与测试文本互为包含；E 池内卫生（uid/目标文本唯一、wav 存在、字段完整）。说话人级（L2）隔离由语料库来源保证（AISHELL-3/LibriTTS vs Seed-TTS 的 DiDiSpeech/Common Voice）并记录于此。

**抽样器**：`python -m audio_dit_quantize.calib.pool sample --pool data/calib_pool/pool_v1.lst --n 32 --seed <k> --out data/calib_pool/sets/rand32_s<k>.lst`（wav 路径按输出目录重写，`load_items` 直接可用）。

**已知局限（记录在案）**：① hardlike 为合成文本：twister 子风格的"词"来自 bigram 挖掘，仍有少量跨词边界的伪词（发音混淆属性正确，语义通顺性不保证）；真实 hard 集 max_char_run p90=6 的字符连跑尾部未覆盖；② en 无 hard 子集（测试集也没有）；③ AISHELL-3 转写无标点，构建时统一补句号；④ 语料域差异（朗读风格 vs 测试集口语域）未量化（原 E1 锚点实验已取消）。

### 3.3 实验推进与统计纪律（2026-07-19 确立）

**背景**：既有经验表明 FlatQuant paper-best 在常规集上已逼近 fp32——我们在**小效应量**领域作战，"很多方向表现不稳定"是统计功效不足的症状，不是玄学。所有推进决策遵守：

1. **余量地图先行**。任何新方向动工前，先算目标 (set, metric) 上的 gap = 量化 − fp32（配对 CI）。**gap 就是一切改进方法的收益上限**；gap ≈ 0 的指标不能区分标定集好坏，从选择/比较信号中剔除。（首轮实测见 §4.2：主战场 = hard CER，SIM 不可动。）
2. **散布 vs gap 的比较才算判读**。组间散布占总 gap 的比例回答"值不值得选"。（2026-07-19 用户决定：**不做 seed 重复对照**，全部按单 `calib_seed=0` 解读，标定实例噪声并入"换随机集"的变差一并处理，不单独剥离。）
3. **设计对照优于回归淘金**。随机组的特征回归（样本少、特征多）只许用于提假设；确认一律用**单因子最大对比构造集**（全 hard vs 全 normal、多说话人 vs 单说话人、早 vs 晚 timestep 等），一次对照仅 2 个作业，功效远高于回归。
4. **优先做无空结果风险的实验**。预算悬崖扫描、最差抽签兜底这类"效应必然随某轴放大"的设计优先；追逐微小均值改善的方向（新 loss 花样等）必须先过设计对照的效应量门槛才继续投入。
5. 既有纪律不变：逐条配对 + paired bootstrap；冻结 base（§2.6）；dev 选、heldtest 报（§3.2）。

### 3.4 路线图

判定门先行，通过后才重投入；全部判定在 1B 模型 + dev 划分上做，3.5B 只在最终确认用。**每一步动工前过一遍 §3.3。**

| ID | 实验 | 内容 | 判定标准 | 状态 |
|---|---|---|---|---|
| **E0** | 工程解锁 | `--calib_lst` 参数化（含 `SEED_CALIB_LST`）；语料下载；候选池 v1 构建+审计（§3.2.1）；dev/heldtest 划分冻结（§3.2）；Phase 0 驱动 `scripts/calib/phase0_sensitivity.sh` + 收集器 | 全管线冒烟跑通 | **完成**（2026-07-19；过程记录见 §5.2） |
| ~~E1~~ | ~~污染溢价锚点~~ | ~~干净池随机 32 条 vs 旧列表基线~~ | — | **已取消**（2026-07-19 用户决定：旧列表已删除，不再与其比较；当时的污染审计数字见本文档的 git 历史版本） |
| **GATE-B** | 敏感性研究（Phase 0） | K=10 组随机 32 条集各"标定+dev 生成+评测"+ fp32 配对参照。这 10 组跑完即转正为论文的**随机 baseline 分布行**与 **best-of-10 参照行** | 组间散布相对总 gap 值得追、且 best-vs-worst 配对显著 → 主线开绿灯 | **首轮开门；终判进行中**——seed 方差实验（s0 × calib_seed 0/1/2）在跑，剥离实例噪声后出最终判决（§4.5 分流） |
| **E2** | 候选集打分器 | v1 三分数（loss_init/influence/Fisher）+ v2 探针耦合分数（coverage/transfer，SelectQ 谱系）。实现：`calib/sensitivity/{probes,score,probe_capture,validate}.py` + 探针集 probe_v1 | 验货门：分数排序须显著复现 14 个已知集的 hard CER 排序（n=14 时 \|rho\|≳0.53） | **关闭（负结果，2026-07-19）**：v1/v2 三族分数均未过门，loss_init 另有复现性红灯（§4.4）。基础设施与探针集保留；Fisher 权重留给 GATE-A（期望调低） |
| **GATE-A** | 损失加权设计对照 | var 加权（反向 chanbal）/ Fisher 通道加权 / Fisher token×通道，各 vs uniform-MSE（同一标定集 rand32_s0、同 calib_seed，配对 bootstrap） | 出现历史 chanbal 那种"全面、方向一致"的效应 → 副线成立；全在噪声内 → 副线收缩为消融 | 未开始（依赖 E2 的 Fisher 行；var 行只需 `--loss varw` 小改动，需用户确认后实现） |
| **P1** | 代理特征 → 假设 → 对照确认 | 相关性提假设（§4.2.3-D）→ 单因子对照确认（§4.3） | 产出通过设计对照确认的特征 | **首轮完成**（§4.3）：线索① 否决、线索③ 方向对但欠功效、线索② 域内成立待预注册测试；**意外确认"语言匹配原则"**（en 显著伤 zh，因果） |
| **P2** | 规则集选择（构成规则路线） | `pool.py select`：0 英文（因果确认）+ hardlike 16/32（已测配比点）+ 说话人最大化（32/32），约束内随机。首实验：2 个新draw（rule32_r0/r1，seed 2000/2001）vs 随机分布(10) vs best-of-10 vs fp32；先行数据点 ctr_hard16（恰好符合规则）hard CER 6.814 = 全部集合第 2 | 两个规则 draw 的 hard CER 稳定落在随机分布的好侧尾部（≤均值−1σ 区域）且不牺牲 zh | **集合已构建+审计 PASS，待跑**（2 作业；解读需结合 seed 判别结果） |
| **P3** | 泛化确认 | 选中集合迁移 SVDQuant；上 3.5B；untouched test 报最终数 | 增益跨方法/跨规模成立 | 未开始 |
| INT8 | W8A8 参照档 | dev 集上 int8（无标定、确定性），做退化阶梯 fp32→W8A8→W4A4 + 配对管线阴性对照 | 预期与 fp32 逐条无差 | 命令已就绪，待空闲卡 |

依赖关系：GATE-B（终判待 seed 对照）→ P1 确认 → P2；E2 与主线并行开发；GATE-A 依赖 E2（var 行除外）；P3 最后。

---

## 4. 实验结果与分析

### 4.1 已确立结论汇总

| 日期 | 结论 | 证据 | 对后续的意义 |
|---|---|---|---|
| ≤2026-07 | 三方法均可跑通 W4A4 并对齐论文最优配置 | `docs/paper-best-alignment.md` | 基线冻结的前提 |
| ≤2026-07 | **chanbal loss（1/方差通道加权）在逐 block 粒度下全面差于普通 MSE；在逐 linear 粒度下明显更好**（均为 best config 下） | 历史实验（旧标定集） | 通道加权是真实杠杆但 1/var 在残差流上方向反了；催生 GATE-A（敏感度加权） |
| ≤2026-07 | 晚期 ODE 步保持激活全精度可恢复 SIM（step-axis 实验，LATE 有效、EARLY 同预算无效） | `scripts/benchmark/benchmark_step_axis_seedtts.sh` | 时间步敏感度不均匀；与 §4.2"SIM 对标定数据不敏感"互证——SIM 靠时间步轴救，不靠数据选择 |
| ≤2026-07 | **FlatQuant paper-best 与 fp32 的差距已很小（常规集），多个改进方向历史上表现不稳定** | 历史实验（用户经验） | 小效应量作战——催生 §3.3 统计纪律；§4.2 实测证实：余量在 hard CER/en WER，zh CER 无成本，SIM 成本恒定 |
| 2026-07-19 | （待 seed 对照确认后转正）hard CER 对标定数据选择敏感：best-vs-worst 配对显著 | §4.2 首轮 GATE-B | 主线的存在性证据 |
| 2026-07-20 | **语言匹配原则（因果，设计对照确认）**：标定集混入 12/32 英文使 zh CER 显著劣化 +0.255；纯 zh 标定集的 zh CER 为全部 run 最佳；反向不对称（加 en 不助 en） | §4.3 en 对照 | 选择方法的硬约束：目标语言轴的标定数据应语言匹配 |
| 2026-07-20 | E2 影响力分数 v1 = 文本长度代理（病理）；loss_init 仅在随机构成域内预测 hard CER（rho −0.66），不外推到构造集 | §4.4 | ~~降级为域内筛选候选~~ **已推翻**（见下一行） |
| 2026-07-19 | **E2 整线关闭（负结果）**：v2 探针耦合分数（coverage/transfer）无信号且 coverage 方向反；loss_init 两轮同代码运行排序翻号（复现性不足，组间动态范围 ±4% 低于 GPU 运行噪声底） | §4.4 v2 验证 | "仅凭模型侧信号廉价选数据"在本设置下不成立；论文作诚实负结果章节；P2 转构成规则路线 |

### 4.2 GATE-B 敏感性研究（Phase 0）——首轮，2026-07-19

**配置**：1B 冻结 paper-best FlatQuant；K=10 组随机 32 条标定集（pool_v1，seed 0–9）+ fp32 配对参照；dev 划分（zh 300 / hard 100 / en 200）；calib_seed=0，无 seed 重复；评测 CER/WER/SIM（MOS 未跑；en SIM 于 2026-07-20 补评并入）。

#### 4.2.1 指标总表（聚合值）

| run | zh CER% | hard CER% | en WER% | zh SIM | hard SIM | en SIM |
|---|---|---|---|---|---|---|
| **fp32** | 1.452 | 6.929 | 2.868 | 0.811 | 0.791 | 0.760 |
| rand32_s0 | 1.255 | 7.475 | 3.422 | 0.809 | 0.787 | 0.751 |
| rand32_s1 | 1.214 | 6.875 | 3.257 | 0.808 | 0.790 | 0.753 |
| rand32_s2 | 1.330 | 7.244 | 3.074 | 0.809 | 0.788 | 0.753 |
| rand32_s3 | 1.367 | 6.991 | 3.366 | 0.808 | 0.789 | 0.752 |
| rand32_s4 | 1.157 | 6.859 | 2.985 | 0.809 | 0.789 | 0.755 |
| rand32_s5 | 1.372 | 7.489 | 3.250 | 0.807 | 0.788 | 0.748 |
| rand32_s6 | 1.343 | 7.426 | 2.806 | 0.807 | 0.788 | 0.751 |
| rand32_s7 | 1.309 | 7.121 | 2.806 | 0.808 | 0.790 | 0.753 |
| rand32_s8 | 1.335 | 6.644 | 3.129 | 0.807 | 0.787 | 0.752 |
| rand32_s9 | 1.168 | 6.853 | 3.148 | 0.810 | 0.789 | 0.753 |
| **10 组 mean±std** | 1.285±0.081 | 7.098±0.300 | 3.124±0.212 | 0.808±0.001 | 0.789±0.001 | 0.752±0.002 |

#### 4.2.2 配对 Δ 表（每组 vs fp32，逐条配对 bootstrap 95% CI；**加粗 = 显著**）

| run | Δzh CER | Δhard CER | Δen WER | Δzh SIM | Δhard SIM | Δen SIM |
|---|---|---|---|---|---|---|
| rand32_s0 | -0.197 [-0.547,+0.169] | **+0.546 [+0.022,+1.118]** | **+0.554 [+0.130,+1.021]** | **-0.0025 [-0.0044,-0.0007]** | **-0.0040 [-0.0069,-0.0013]** | **-0.0088 [-0.0121,-0.0056]** |
| rand32_s1 | -0.237 [-0.576,+0.069] | -0.055 [-0.666,+0.551] | +0.389 [-0.035,+0.864] | **-0.0035 [-0.0054,-0.0016]** | -0.0009 [-0.0041,+0.0022] | **-0.0065 [-0.0103,-0.0028]** |
| rand32_s2 | -0.121 [-0.444,+0.170] | +0.315 [-0.364,+1.130] | +0.206 [-0.050,+0.499] | **-0.0024 [-0.0042,-0.0006]** | -0.0032 [-0.0068,+0.0004] | **-0.0070 [-0.0102,-0.0040]** |
| rand32_s3 | -0.085 [-0.291,+0.111] | +0.062 [-0.545,+0.715] | **+0.498 [+0.187,+0.856]** | **-0.0034 [-0.0052,-0.0016]** | -0.0018 [-0.0055,+0.0017] | **-0.0077 [-0.0111,-0.0043]** |
| rand32_s4 | -0.294 [-0.638,+0.022] | -0.071 [-0.699,+0.623] | +0.118 [-0.412,+0.628] | **-0.0025 [-0.0044,-0.0006]** | -0.0020 [-0.0049,+0.0008] | **-0.0050 [-0.0081,-0.0019]** |
| rand32_s5 | -0.079 [-0.478,+0.303] | +0.560 [-0.121,+1.291] | +0.382 [-0.014,+0.838] | **-0.0041 [-0.0060,-0.0023]** | -0.0032 [-0.0069,+0.0004] | **-0.0114 [-0.0151,-0.0078]** |
| rand32_s6 | -0.109 [-0.309,+0.079] | +0.496 [-0.086,+1.116] | -0.062 [-0.364,+0.223] | **-0.0040 [-0.0059,-0.0021]** | -0.0035 [-0.0075,+0.0001] | **-0.0091 [-0.0123,-0.0058]** |
| rand32_s7 | -0.143 [-0.387,+0.077] | +0.192 [-0.434,+0.818] | -0.061 [-0.485,+0.341] | **-0.0034 [-0.0052,-0.0015]** | -0.0008 [-0.0040,+0.0025] | **-0.0067 [-0.0095,-0.0039]** |
| rand32_s8 | -0.117 [-0.443,+0.178] | -0.285 [-0.792,+0.205] | +0.262 [-0.068,+0.611] | **-0.0047 [-0.0065,-0.0029]** | **-0.0038 [-0.0068,-0.0008]** | **-0.0078 [-0.0111,-0.0044]** |
| rand32_s9 | **-0.284 [-0.571,-0.041]** | -0.077 [-0.703,+0.593] | +0.280 [-0.092,+0.675] | -0.0017 [-0.0035,+0.0002] | -0.0021 [-0.0054,+0.0010] | **-0.0071 [-0.0106,-0.0037]** |

#### 4.2.3 分析

**A. 余量地图（三层判读的 gap 层与散布层）**

| 轴 | fp32 | 平均量化成本 (mean Δ) | 组间散布 std / oracle gap | 判读 |
|---|---|---|---|---|
| zh CER | 1.452 | **−0.167（变好）** | 0.081 / 0.215 | 常规 zh 量化零成本甚至微升：10/10 组点估计为负（符号检验 p≈0.002），s9 单独显著 |
| **hard CER** | 6.929 | +0.169 | **0.300 / 0.845** | **主战场**：散布是平均成本的 5 倍；s8 优于 fp32（−0.285），s0/s5 劣化 ~+0.55 |
| en WER | 2.868 | +0.257 | 0.212 / 0.616 | 次战场，方向一致的中等敏感 |
| zh SIM | 0.811 | −0.003 | 0.001 / 0.003 | **SIM 成本普遍（9/10 显著）、恒定、对标定数据不敏感**——数据选择救不了 SIM |
| hard SIM | 0.791 | −0.003 | 0.001 / 0.003 | 同上 |
| en SIM | 0.760 | **−0.008（三集中最重，10/10 显著）** | 0.0017 / 0.0064 | SIM 不敏感结论在三集闭环；en SIM 成本约为 zh 的 2.5 倍，数据敏感性略高于 zh/hard 但仍弱（rho(en_wer, en_sim)=−0.20） |

**B. best-vs-worst 逐条配对**（综合排名 best=s4，worst=s5；事后选择，有 winner's curse，待确认）：hard CER **−0.631 [−1.228, −0.058] 显著**；其余轴 ns。→ "换一组标定数据，hard 可懂度可测地变化"，杠杆存在性的直接证据。

**C. 各向异性与轴间关系**：zh CER 与 zh SIM 跨组 rho=−0.66——好组两头都好，**无取舍**（"有些标定集整体更好"）；en 轴与 zh 轴基本解耦（rho≈0）。

**D. P1 特征假设**（n=10，|rho|≥0.64≈p0.05 参考线，未校正，**仅提假设**）：

| 特征 | vs hard CER | vs zh CER | 假设与后续 |
|---|---|---|---|
| `n_en`（标定集中英文条数） | **−0.65** | −0.53 | 英文条目多 → hard 更好？反直觉，**头号设计对照候选**（0 en vs 高 en） |
| `sum_loss_first`（初始标定 loss） | **−0.65** | −0.02 | 难拟合的数据 → hard 更好；**标定前可算的纯模型侧信号**，若确认即为选择准则 |
| `mean_uniqchar`（文本唯一字符比） | +0.61 | +0.20 | 重复性强的标定文本 → hard 更好（符合直觉，与 hardlike 供给相关） |
| `mean_tgt_len`（目标文本长度） | −0.28 | −0.61 | 长文本 → zh 更好 |

#### 4.2.4 判决与下一步

**开门**（2026-07-19，用户决定采用单 seed 协议、不做 seed 对照）：

1. 杠杆在 **hard CER** 上真实存在：最好组 vs 最差组逐条配对差 −0.63 [−1.23, −0.06]，显著；en WER 次之；
2. **SIM 不可由数据选择改善**（10 组散布仅 0.001）——重要负结果，主张范围收窄到"hard/en 可懂度"，SIM 交给时间步轴（step-axis 结论）；
3. zh 常规集量化零成本（甚至一致微升），从选择信号中剔除；
4. 10 组转正为论文的**随机 baseline 分布行**；**s4 为 best-of-10**，`data/calib_pool/sets/rand32_s4.lst` 即日起作为**临时标准标定集**（后续 baseline 类实验默认用它，直到被 P2 产物取代）。

下一步（按优先级）：① P1 三个假设的**设计对照**（en 条数、文本重复性、`sum_loss_first` 信号，各 2 作业）；② E2 敏感度模块开发；

### 4.3 P1 设计对照（en 条数 / hardlike 比例）——2026-07-20

**配置**：两对单因子对照集（`calib/pool.py contrast`，seed=1000）：en 对（A=32 纯 zh，B=20 共享 zh + 12 en）；hardlike 对（A=32 zh_normal，B=16 共享 normal + 16 hardlike）。其余协议同 §4.2。

#### 4.3.1 指标总表

| run | zh CER% | hard CER% | en WER% | zh SIM | hard SIM | en SIM |
|---|---|---|---|---|---|---|
| p0_fp32（参照） | 1.452 | 6.929 | 2.868 | 0.811 | 0.791 | 0.760 |
| p1c_en0 | **1.140** | 7.414 | 3.011 | 0.808 | 0.789 | 0.753 |
| p1c_en12 | 1.395 | 7.307 | 3.050 | 0.807 | 0.789 | 0.751 |
| p1c_hard0 | 1.306 | 7.051 | 3.170 | 0.806 | 0.787 | 0.752 |
| p1c_hard16 | 1.388 | **6.814** | 2.845 | 0.807 | 0.787 | 0.753 |

#### 4.3.2 对内配对 Δ（B−A，95% CI；加粗=显著）

| 对照 | Δzh CER | Δhard CER | Δen WER | ΔSIM（三集） |
|---|---|---|---|---|
| en12−en0 | **+0.255 [+0.009,+0.527]** | −0.106 [−0.791,+0.607] | +0.039 [−0.423,+0.520] | 全部 ns |
| hard16−hard0 | +0.082 [−0.284,+0.477] | −0.238 [−0.825,+0.357] | −0.324 [−0.674,+0.008] | 全部 ns |

#### 4.3.3 分析与判决

- **线索①（en 多→hard 好）未获确认**：hard CER 点估计 −0.106，远小于相关性暗示的幅度且 ns。原 rho=−0.65 判定为淘金假象/混杂。
- **线索③（hardlike→hard 好）方向正确但未达显著**：−0.238 [−0.825,+0.357]。效应若真实约 0.2–0.3pp，在 n=100 hard 条目下功效不足。
- **意外的真发现：en 条目显著损害 zh CER（+0.255，SIG）**——标定语言构成对目标语言可懂度存在**因果**影响（纯 zh 集的 zh CER 1.140 为全部 16 个 run 最佳）。方向与假设相反、轴不同，但这是本项目第一个通过设计对照的因果结论：**语言匹配原则**。反向不对称：加 en 不改善 en WER（ns）。

### 4.4 E2 打分器验证——2026-07-20

**实现自检通过**：block 级 init loss 与真实训练日志的 loss_first 相关 0.934、尺度比 0.99（计算正确）；set 级仅 0.45——因为集合间 init loss 的动态范围本身很窄（±4%），两种测量的噪声都不小。

**验货门未过**（n=14，|rho| 需 ≳0.53）：

| 分数 | all14 vs hard CER | 仅 rand10 | 仅对照 4 | 判读 |
|---|---|---|---|---|
| sum_loss_init | −0.31 | **−0.66** | +0.20 | 随机构成域内可复现（独立测量重现了 P0 的 −0.65，非日志噪声假象），但**不能外推到构造集**——是域内筛选信号，不是因果准则 |
| mean_influence | +0.05 | +0.45 | −0.20 | **病理确认**：与目标文本长度 rho=+0.43（本质是长度代理，per-token 化后 CV 从 0.83 降到 0.30 仍残留 0.36）——v1 影响力分数作废 |

**全答案轴矩阵**（2026-07-20 补充，sum_loss_init 的 Spearman；influence 族全轴均无信号不再列）：

| 答案轴 | all14 | rand10 | ctr4 |
|---|---|---|---|
| zh CER | −0.10 | −0.33 | +0.00 |
| **hard CER** | −0.31 | **−0.66** | +0.20 |
| en WER | +0.24 | +0.09 | +1.00* |
| SIM（三集） | +0.09~+0.37 | +0.19~+0.48 | ±0.8* |

*ctr4 列 n=4，±1/±0.8 属小样本伪象，不解读。SIM 各轴组间散布≈噪声（std 0.001–0.002），其排序相关无意义。

**解读**：loss_init 是 **hard 专用信号**——不预测 zh CER（−0.33 弱）和 en WER（+0.09≈0）。这与余量地图自洽：zh 无退化可预测（全部 run 优于 fp32）、en 由语言匹配因子主导（§4.3），loss_init 追踪的恰是量化损伤真正所在的轴。预注册测试的结局指标因此**写死为 hard CER**，其余轴仅作次要报告。

**v1 判决**：不能进 P2。influence 病理作废；loss_init 暂定为"随机域内、hard 轴专用"筛选候选（后被 v2 轮的复现性检验推翻，见下）。

**v2 重新出题（动机：v1 的题目缺陷——给"教材难度"打分而非"预测考分"、集合分数误用逐条平均、探针方向各向同性）**：新增两个把候选集 S 与固定**难例探针集 P** 耦合的分数——`mean_coverage`（设计 C，SelectQ 谱系：S 的逐通道激活范围对 P 的覆盖率；覆盖是并集运算，天然集合级）与 `sum_transfer_loss`（设计 A，交叉验证逻辑：量化器用 S 的统计初始化、在 P 上测重建误差）。探针集 probe_v1：24 条（8 常规+16 新生成难例），prompt 全部来自池外未用的 98 个说话人，与候选、测试集零重叠（审计 PASS）。

**v2 验证结果（14 集重打分）：仍未过门，并暴露复现性红灯**：

| 分数 | vs hard CER (all14) | 判读 |
|---|---|---|
| sum_transfer_loss | +0.06 | 无信号 |
| mean_coverage | +0.21 | 无信号且**方向反**（预期负） |
| sum_loss_init（v2 轮重算） | **+0.24（v1 轮为 −0.31，翻号）** | **复现性红灯**：同代码同 seed 两轮运行排序翻转，而确定性的 influence 逐位复现——set 级 loss_init 动态范围（±4%）低于 GPU 运行间噪声底，v1 轮 rand10 的 −0.66 不可依赖 |

**E2 终判（2026-07-19）：整线关闭。** 三族分数（难度/影响力/覆盖-迁移）全部未过验货门，且唯一有过信号迹象的 loss_init 连自身复现都不过关；原计划的"loss_init 预注册筛选测试"因前提丧失而撤销。"仅凭模型侧信号廉价选择标定数据"在本设置下为**负结果**（论文可作诚实分析章节：三种机制对齐的设计逐一证伪 + 复现性教训）。遗留资产：Fisher 通道权重（GATE-A 用途未被此结论波及，但期望相应调低）；探针集与打分基础设施保留备查。

### 4.5 综合判读与当前状态（2026-07-19 更新）

**结论分级总表**（论文口径，与 §4.1 对齐；"方向一致但不显著"不得表述为"验证有效"）：

| 结论 | 等级 | 证据 |
|---|---|---|
| 标定数据选择在 hard CER 上存在杠杆；暴力选择（best-of-K）作为参照策略成立 | 成立*（星号=待 seed 判别剥离实例噪声） | §4.2 best-vs-worst 配对显著 |
| 语言匹配原则（en 条目因果性损害 zh CER） | **因果确认** | §4.3 设计对照显著 |
| hardlike 配比 → hard 更好 | 方向性支持（未显著，欠功效） | §4.3 点估计 −0.238，CI 含零 |
| en 条目 → hard 更好 | 否决（淘金假象） | §4.3 |
| SIM 成本恒定、不受数据选择影响（三集闭环） | 确认（负结果） | §4.2 |
| 模型侧廉价打分选数据（难度/影响力/覆盖-迁移三族） | **负结果，E2 关闭** | §4.4 |

**进行中的最终判别**：seed 方差实验（rand32_s0 × calib_seed 0/1/2，2 个新作业）——回答"组间散布 0.845 里实例噪声占比"。分流：噪声占大头 → "选数据"杠杆降级，故事转向"语言匹配因果结论 + 标定不稳定性刻画 + 时间步轴（step-axis 先验）"；噪声小 → 数据效应真实但因子弥散，P2 走**构成规则路线**（语言匹配硬约束 + hardlike 配比 + 说话人覆盖），对照随机分布与 best-of-10，heldtest 终验。

---

## 5. 其他

### 5.1 维护指南

- **加一个实验**：先在 §3.4 路线图领 ID；跑完把**指标表 + 配对 Δ + 分析 + 判决**写成 §4 的一个小节（模板照 §4.2）；工程/协议变更记入 §5.2；改变项目认知的结论提升到 §4.1。
- **不许删历史**：错误结论标注"已推翻 + 指向推翻它的实验"，保留原文。
- **标定列表机制**：所有标定入口用 `--calib_lst`（或 `SEED_CALIB_LST` 环境变量）显式指定列表；默认路径指向已删除的旧文件（有意为之——逼新实验显式选择干净列表）。列表中 wav 路径按"相对列表文件所在目录"解析（`load_items` 行为）。

### 5.2 工程与协议变更记录（living，按时间追加）

| 日期 | 事项 | 要点 |
|---|---|---|
| 2026-07-18 | 候选池 v1 构建 | 376 条（zh_normal 240 / zh_hardlike 60 / en_normal 76），158 说话人，71MB 自包含；审计 PASS（0 污染）；prompt 时长 p10/50/90=4.4/4.7/5.2s、zh 语速 p50=3.8 字/秒、zh 性别 202F/98M、口音 north 224/south 76；en 缺 4 条（2 个说话人无合格时长句）；抽样器 + `load_calib_items` 端到端验证通过 |
| 2026-07-18 | hard 子集推翻重做 | 初版字符汤生成器与真实 hardcase 结构严重错位（max_char_run p50 4 vs 1、标点密度减半）；按真实分类学（词级绕口令/重复/长句）重写为三子风格后 p50 对齐：len 42.5↔51、max_char_run 1↔1、uniq_ratio 0.39↔0.44、phrase_rep 4↔5、punct 0.10↔0.10。**教训：合成难例必须先量化真实难例的结构分布** |
| 2026-07-18 | dev/heldtest 划分冻结 | seed=20260718：zh 300/1720、hard 100/300、en 200/888；uid 清单入库 `data/splits/`，冻结语义验证通过；gen/sharding/eval 三处注册 `*_dev/*_heldtest` |
| 2026-07-19 | 冒烟测试 + 修复 | 管线全通；修复：① 失败评测遗留 nan 文件会被续跑误判为完成（`job_done` 加 nan 检测+清除）② 快慢作业同波 GPU 空转（fp32 挪队尾）。~~nan 根因 = 共享机 ASR 被挤显存~~（**已推翻**，见 2026-07-20 行：真因是 nan 检测本身的假阳性） |
| 2026-07-20 | **nan 假阳性根因修复**（本机复现确认） | `grep -qi nan` 扫全文件，被 en 结果里 "conso**nan**t/Lieute**nan**t" 等普通英文词触发：① 评测守卫在每个 en_dev 文本指标后必然假警报退出 → **en SIM 反复丢失的真正根因**（非 Whisper/显存）；② `job_done` 同一模式误删有效 en 结果并把全部已完成作业判为未完成 → 续跑失效。修复：两处 nan 检测收窄到末行聚合值（`tail -1`），本机对真实结果文件验证 SKIP/PENDING 判定正确。教训入档：**对含自然语言文本的文件做子串匹配守卫必须锚定到结构化行** |
| 2026-07-19 | 协议与工具调整 | MOS 移出默认评测（可事后补）；seed 重复改顺序设计（散布可疑再补）；驱动加心跳/逐作业状态/完成即报分（`STATUS_INTERVAL`）；标定新增逐 block loss 记录 → `gen/<tag>/calib_block_losses.json`（P1 免费特征）。**模型实例不落盘**：标定含 CUDA 非确定性（attention backward），重跑得统计等价非逐位相同实例；报告数字绑定已落盘 wav/分数；需要具体模型时对其列表 `--save_model` 一次转正 |
| 2026-07-19 | GATE-B K=10 完成 | 1B，4 GPU，单作业 ≈ 标定 24min + 生成 55min + 评测；结果与分析 → §4.2；en_dev SIM 文件缺失待补 |
| 2026-07-19 | seed 方差判别实验启动 | rand32_s0 × calib_seed 1/2（2 作业，判决规则 §4.5）。期间的续跑事故与恢复：旧 nan-bug 在上一轮启动时已删掉 11 个 en WER 结果文件 → 部分作业被白白重跑一轮（wav 未受损、结果等价重写）；缺失文件从分析机的冻结副本 scp 恢复后，修复版续跑判定验证正常（11 个 `=` + 2 个 `*`） |

### 5.3 命令速查

- 环境：`source env.sh`
- Phase 0：`GPUS=... K=10 SEED_REPEATS="" bash scripts/calib/phase0_sensitivity.sh 1b`（幂等，失败后原命令续跑；seed 对照 `SEED_REPEATS="1 2"`）
- INT8 参照档：`GPUS=... EVAL_METRICS="wer cer sim" bash scripts/benchmark/benchmark_int8_seedtts.sh 1b "zh_dev,hard_dev,en_dev"`
- MOS 事后补：`bash scripts/evaluate_seedtts_metrics.sh <gen_root> <tag> "zh_dev hard_dev en_dev" mos`
- 配对显著性：`python -m audio_dit_quantize.paired_bootstrap --metric cer|sim --a <fp32结果> --b <方法结果>`
- E2 探针（一次性）：`python -m audio_dit_quantize.calib.pool probe --pool data/calib_pool/pool_v1.lst`（CPU 建集）→ `python -m audio_dit_quantize.calib.sensitivity.probe_capture --probe_lst data/calib_pool/probe/probe_v1.lst --out data/calib_pool/probe/probe_capture.pt`（GPU ~5 min）
- E2 打分：`python -m audio_dit_quantize.calib.sensitivity.score --calib_lst <lst> --out data/calib_pool/scores_v2/<名字> --probe_capture data/calib_pool/probe/probe_capture.pt`（单集 ~15 min GPU；不带 --probe_capture 则只算 v1 分数）；校验：`python -m audio_dit_quantize.calib.sensitivity.validate --scores_dir data/calib_pool/scores_v2`
- 基线冒烟：`LIMIT=1 bash scripts/benchmark/benchmark_flatquant_best_seedtts.sh 1b hard`

### 5.4 关键文件索引

方法配置对齐 `docs/paper-best-alignment.md`；FlatQuant 标定 `src/audio_dit_quantize/flatquant_best.py`；标定数据研究线 `src/audio_dit_quantize/calib/`（pool / audit / dev_split / phase0_collect）；路径/数据集常量 `src/audio_dit_quantize/paths.py`；配对统计 `src/audio_dit_quantize/paired_bootstrap.py`；模型仓库 `../LongCat-AudioDiT`（`audiodit/` 为架构定义）。
