"""Calibration-data research line (docs/experiments.md): candidate pool construction (pool),
contamination audit (audit), frozen dev/heldtest split (dev_split), Phase-0 collection
(phase0_collect). Method/eval modules stay at the package top level — saved .pt pickles
reference their class paths, so they must not move."""
